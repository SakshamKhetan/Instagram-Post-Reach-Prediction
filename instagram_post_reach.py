# -*- coding: utf-8 -*-
"""Instagram Post Reach.ipynb

Automatically generated by Colaboratory.

Original file is located at
    https://colab.research.google.com/drive/1j8l8Tl8hmcA_gG5EGEYmaxWhb_TCgEgF
"""

import pandas as pd

df = pd.read_csv('instagram_reach.csv').drop(['Unnamed: 0', 'S.No'],axis=1)
df.head()

new = []
for i in df['Time since posted']:
  new.append(int(i.replace(' hours','')))

df['Time since posted'] = new

from sklearn.linear_model import LinearRegression
from sklearn.model_selection import train_test_split

X= df[['Followers','Time since posted']]
Y = df['Likes']

x_train,x_test,y_train,y_test = train_test_split(X,Y,test_size= 0.2)
model = LinearRegression().fit(x_train,y_train)
print(model.score(x_test,y_test))

y_pred = model.predict(x_test)
y_pred

from sklearn.metrics import mean_squared_error
MSE = mean_squared_error(y_test,y_pred)
round(MSE,2)